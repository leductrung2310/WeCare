import 'dart:html';

import 'package:flutter/material.dart';

class ProfileInfoDateField extends StatelessWidget {
  const ProfileInfoDateField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("");
  }
}
